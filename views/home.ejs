<%- include('partials/header.ejs') %>

<h1>Hello!</h1>
<p><%=contentHome%></p>
 
<% if(posts.length != 0){ %>
<% posts.forEach(function(post) { %>
    <h1><%= post.title %></h1>
    <p><%= post.content.substring(0, 100) +"..." %><span><a href="/posts/<%= post.title %>">Read more</a></span></p>
    
<% }); %> 
<% }else{} %>        


<%- include('partials/footer.ejs') %>